import React from "react";
import ReactDOM from "react-dom/client";
import SecondCastModel from "../second_cast_financial_model_1.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SecondCastModel />
  </React.StrictMode>
);
